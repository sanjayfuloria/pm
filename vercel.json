{
  "buildCommand": "cd frontend && npm install && npm run build && cp -r out ../public",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/index.py" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
